<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.chinarewards.qqgbvpn</groupId>
	<artifactId>main-packaging-debian</artifactId>
	<packaging>pom</packaging>
	<name>POSNetv2 Server Packaging (Debian)</name>
	<parent>
		<groupId>com.chinarewards.qqgbvpn</groupId>
		<artifactId>main-packaging</artifactId>
		<version>0.1.2-RC3</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<dependencies>
		<dependency>
			<groupId>com.chinarewards.qqgbvpn</groupId>
			<artifactId>main</artifactId>
			<classifier>bin</classifier>
			<type>zip</type>
			<version>${project.version}</version>
		</dependency>
	</dependencies>
	<properties>
		<pkgname>posnet2-server</pkgname>
		<dbdeploy.version>3.0M3</dbdeploy.version>
	</properties>
	<build>
		<extensions>
			<extension>
				<groupId>org.apache.maven.wagon</groupId>
				<artifactId>wagon-ssh</artifactId>
				<version>1.0-beta-7</version>
			</extension>
		</extensions>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.2</version>
				<executions>
					<execution>
						<id>copy</id>
						<phase>package</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>com.dbdeploy</groupId>
									<artifactId>dbdeploy-cli</artifactId>
									<version>${dbdeploy.version}</version>
									<type>jar</type>
									<outputDirectory>${project.build.directory}/bin</outputDirectory>
									<destFileName>dbdeploy-cli.jar</destFileName>
								</artifactItem>
								<artifactItem>
									<groupId>com.dbdeploy</groupId>
									<artifactId>dbdeploy-core</artifactId>
									<version>${dbdeploy.version}</version>
									<type>jar</type>
									<outputDirectory>${project.build.directory}/bin</outputDirectory>
									<destFileName>dbdeploy-core.jar</destFileName>
								</artifactItem>
								<artifactItem>
									<groupId>com.dbdeploy</groupId>
									<artifactId>dbdeploy-ant</artifactId>
									<version>${dbdeploy.version}</version>
									<type>jar</type>
									<outputDirectory>${project.build.directory}/bin</outputDirectory>
									<destFileName>dbdeploy-ant.jar</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
					<execution>
						<id>copy-dependencies</id>
						<phase>package</phase>
						<goals>
							<goal>unpack-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<excludeTransitive>true</excludeTransitive>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- build from source (0.9-SNAPSHOT) which has .changes file generation -->
				<artifactId>jdeb</artifactId>
				<groupId>org.vafer</groupId>
				<version>0.9.CR</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>jdeb</goal>
						</goals>
						<configuration>
							<!-- must use this pattern -->
							<deb>${project.build.directory}/${pkgname}_${project.version}_all.deb</deb>
							<!-- points to an empty file -->
							<changesIn>${basedir}/CHANGES.txt</changesIn>
							<changesOut>${project.build.directory}/${pkgname}_${project.version}.changes</changesOut>
							<changesSave>${project.build.directory}/CHANGES.save</changesSave>
							<attach>false</attach>
							<dataSet>
								<!-- Just the main .jar and lib/ jars -->
								<data>
									<src>${project.build.directory}/main-${project.version}/qqgbvpn.jar</src>
									<type>file</type>
									<mapper>
										<type>prefix</type>
										<prefix>/usr/share/posnet2</prefix>
										<filemode>0600</filemode>
									</mapper>
								</data>
								<!-- Just the main .jar and lib/ jars -->
								<data>
									<src>${project.build.directory}/bin</src>
									<type>directory</type>
									<mapper>
										<type>prefix</type>
										<prefix>/usr/share/posnet2/bin</prefix>
										<filemode>0644</filemode>
									</mapper>
								</data>
								<data>
									<src>${project.build.directory}/main-${project.version}/db</src>
									<type>directory</type>
									<mapper>
										<type>prefix</type>
										<prefix>/usr/share/posnet2/db</prefix>
										<filemode>0644</filemode>
									</mapper>
								</data>
								<data>
									<src>${project.build.directory}/main-${project.version}/lib</src>
									<type>directory</type>
									<mapper>
										<type>prefix</type>
										<prefix>/usr/share/posnet2/lib</prefix>
										<filemode>0644</filemode>
									</mapper>
								</data>
								<data>
									<src>${project.build.directory}/main-${project.version}/conf</src>
									<type>directory</type>
									<mapper>
										<type>prefix</type>
										<prefix>/etc/posnet2</prefix>
										<filemode>0600</filemode>
									</mapper>
								</data>
								<data>
									<src>${basedir}/scripts/posnet2</src>
									<type>file</type>
									<mapper>
										<type>perm</type>
										<prefix>/etc/init.d</prefix>
										<filemode>0755</filemode>
									</mapper>
								</data>
							</dataSet>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>wagon-maven-plugin</artifactId>
				<version>1.0-beta-3</version>
				<configuration>
					<fromDir>${project.build.directory}</fromDir>
					<includes>*.deb,*.changes</includes>
					<serverId>repository</serverId>
					<url>${repo.incoming.url}</url>
				</configuration>
				<executions>
					<execution>
						<id>upload-deb-to-server</id>
						<phase>deploy</phase>
						<goals>
							<goal>upload</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
