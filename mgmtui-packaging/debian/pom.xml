<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.chinarewards.qqgbvpn</groupId>
	<artifactId>mgmtui-packaging-debian</artifactId>
	<version>0.2.0-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>POSNetv2 Server Management UI Packaging (Debian)</name>
	<parent>
		<groupId>com.chinarewards.qqgbvpn</groupId>
		<artifactId>mgmtui-packaging</artifactId>
		<version>0.2.0-SNAPSHOT</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<properties>
		<!-- Debian package name -->
		<pkgname>posnet2-mgmtui</pkgname>
		<home.dir>/usr/share/posnet2-mgmtui</home.dir>
	</properties>
	<dependencies>
		<dependency>
			<groupId>com.chinarewards.qqgbvpn</groupId>
			<artifactId>mgmtui</artifactId>
			<version>${project.version}</version>
			<type>war</type>
		</dependency>
	</dependencies>
	<build>
		<extensions>
			<extension>
				<groupId>org.apache.maven.wagon</groupId>
				<artifactId>wagon-ssh</artifactId>
				<version>1.0-beta-7</version>
			</extension>
		</extensions>
		<plugins>
			<!-- Copy the .war file to our build directory. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.2</version>
				<executions>
					<execution>
						<id>copy</id>
						<phase>package</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<excludeTransitive>true</excludeTransitive>
						</configuration>
						<!--
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>com.chinarewards.qqgbvpn</groupId>
									<artifactId>mgmtui</artifactId>
									<version>${project.version}</version>
									<type>war</type>
									<outputDirectory>${project.build.directory}</outputDirectory>
									<destFileName>mgmtui-${project.version}.war</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
						-->
					</execution>
				</executions>
			</plugin>
			
			<!-- Build the .deb Debian package using jDeb. -->
			<plugin>
				<artifactId>jdeb</artifactId>
				<groupId>org.vafer</groupId>
				<version>0.9.CR</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>jdeb</goal>
						</goals>
						<configuration>
							<deb>${project.build.directory}/${pkgname}_${project.version}_all.deb</deb>
							<!-- points to an empty file -->
							<changesIn>${basedir}/CHANGES.txt</changesIn>
							<changesOut>${project.build.directory}/${pkgname}_${project.version}.changes</changesOut>
							<changesSave>${project.build.directory}/CHANGES.save</changesSave>
							<attach>false</attach>
							<dataSet>
								<!-- Application: .war webapp -->
								<data>
									<src>${project.build.directory}/mgmtui-${project.version}.war</src>
									<type>file</type>
									<mapper>
										<type>prefix</type>
										<prefix>${home.dir}/lib</prefix>
									</mapper>
								</data>
								<!-- Application server specific scripts -->
								<data>
									<src>${basedir}/src/etc</src>
									<type>directory</type>
									<mapper>
										<type>prefix</type>
										<prefix>/etc/${pkgname}</prefix>
									</mapper>
								</data>
								<!-- /etc/init.d script. -->
								<data>
									<src>${basedir}/src/scripts/posnet2-mgmtui</src>
									<type>file</type>
									<mapper>
										<type>perm</type>
										<prefix>/etc/init.d</prefix>
										<filemode>0755</filemode>
									</mapper>
								</data>
							</dataSet>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.6</version>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>wagon-maven-plugin</artifactId>
				<version>1.0-beta-3</version>
				<configuration>
					<fromDir>${project.build.directory}</fromDir>
					<includes>*.deb,*.changes</includes>
					<serverId>repository</serverId>
					<url>${repo.incoming.url}</url>
				</configuration>
				<executions>
					<execution>
						<id>upload-deb-to-server</id>
						<phase>deploy</phase>
						<goals>
							<goal>upload</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
